import ShareBtn from "@/components/ShareBtn";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ShareBtn url={""} />
    </div>
  );
};

export default page;
